<%= form_with model: comment do |f| %>
  <div class="field">
    <%= f.label "New comment" %>
    <%= f.rich_text_area :content %>
    <%= f.hidden_field :todo_id, value: comment.todo.id %>
    <%= f.submit %>
  </div>
<% end %>
